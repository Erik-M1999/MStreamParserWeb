node_modules/
dist/
log/
*.log

# Local SQLite database (Prisma migrations ARE committed; the db file is not).
prisma/*.db
prisma/*.db-journal

# Secrets — NEVER commit. Only .env.example (placeholders) is tracked.
.env
.env.*
!.env.example
