/TEMP.*

node_modules/
*.log

# Secrets — NEVER commit. Only .env.example (placeholders) is tracked.
.env
.env.*
!.env.example

backend/dist/
backend/log/

# Staged frontend build (produced by `npm run build:web`); shipped separately.
backend/public/

# Coverage report is committed for submission (grading asks for it in the repo).
# Regenerate any time with `npm run test:coverage` from backend/.
# backend/coverage/

backend/prisma/*.db
backend/prisma/*.db-journal

frontend/.next/
frontend/out/
frontend/build/
frontend/next-env.d.ts

frontend/cypress/videos/
frontend/cypress/screenshots/
frontend/cypress/downloads/
