// Base URL of the Express backend. Used both server-side (Server Components)
// and client-side (links the browser navigates to, e.g. the Spotify login).
// 127.0.0.1 (not "localhost") to match the Spotify loopback redirect rule.
export const BACKEND_URL = "http://127.0.0.1:3000";
